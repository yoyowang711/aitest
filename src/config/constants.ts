export const PROJECT_TYPES = {
  app: { label: "App", emoji: "📱" },
  web: { label: "Web", emoji: "🌐" },
  backend: { label: "后台", emoji: "⚙️" },
} as const;

export const CHECKLIST_STATUS_LABELS = {
  todo: "待检查",
  done: "已通过",
  n_a: "不适用",
} as const;
